from tkinter import *

class point(self, x, y):
    self.x = x
    self.y = y

class line(self, x, y):

    def draw():
        self.canvas = Canvas(fill_color="red")
        self. canvas.create_line(
            x1, y1, x2, y2, fill=fill_color, width=2
        )
        